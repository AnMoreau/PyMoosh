
# The context file is not necessary anymore!
# Instead, before testing install locally the library with
# $ python -m pip install -e .
# (from the command line, when in the root of the PyMoosh directory)