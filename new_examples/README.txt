
(For previous users) The context file is not necessary anymore!

For all users:
Before testing install locally the library with
$ python -m pip install -e .
(from the command line, when in the root of the PyMoosh directory)